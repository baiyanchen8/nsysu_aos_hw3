# Mood Diary (心情日記)

這是一個基於 Flutter 開發的跨平台心情日記應用程式。
核心設計理念為「本地優先 (Local-First)」，確保使用者隱私，並結合 AI 技術提供即時的情緒回饋（雞湯語錄）。

## ✨ 主要功能 (Features)

*   **📅 日曆視圖**: 直觀查看每月的日記狀態與心情分佈。
*   **📝 富文本編輯**: 支援 Markdown 語法與插入圖片，記錄生活點滴。
*   **😊 心情追蹤**: 紀錄每日心情與具體 Emoji，視覺化情緒變化。
*   **🤖 AI 雞湯回饋**:
    *   **本地模式**: 內建精選語錄庫。
    *   **AI 模式**: 支援串接 OpenAI / Google Gemini / Local LLM (LM Studio)，根據日記內容生成專屬安慰。
*   **📦 資料管理**:
    *   **備份與還原**: 支援匯出包含圖片的 ZIP 備份檔，輕鬆遷移資料。
    *   **批量管理**: 方便清理或整理舊日記。
*   **📚 語錄擴充**: 可匯入外部 JSON 擴充雞湯庫。

## 📂 資料夾結構說明 (Project Structure)

本專案的主要程式碼位於 `flutter_app1` 資料夾中：

*   **`flutter_app1/`**: Flutter 專案根目錄。
    *   **`lib/`**: 應用程式核心程式碼。
        *   `data/`: 資料層，包含資料庫模型 (ObjectBox)、備份服務、AI 服務邏輯。
        *   `ui/`: 介面層，包含所有頁面 (Screen) 與共用元件 (Widget)。
        *   `providers/`: 狀態管理層 (Riverpod)，負責跨頁面狀態共享。
    *   **`assets/`**: 靜態資源檔案 (如預設的 `quotes.json` 雞湯語錄)。
    *   **`android/`, `ios/`, `windows/`, `linux/`**: 各平台的原生配置檔案。

*   **`json_file/`**: 存放外部擴充的雞湯語錄 JSON 檔案。
    *   包含多種分類的語錄 (如 `quotes.json`)，可透過 App 設定頁面匯入。
    *   未來將持續更新更多語錄集。

*   **`release/`**: 存放 android 的 apk files。
    *   android : apks
    *   ios : 只有 `Readme` 介紹如何安裝。 
    
## 🤖 如何設定 AI 雞湯功能 (AI Configuration)

本應用程式支援多種 AI 模型來源，請依照以下步驟設定：

1.  進入 **設定 (Settings)** 頁面。
2.  開啟 **「啟用 AI 遠端生成模式」** 開關。
3.  在 **「AI 供應商設定」** 中選擇您想使用的服務：

### 1. Google Gemini (推薦)
*   **取得金鑰**: 前往 Google AI Studio 申請免費的 API Key。
*   **取得模型**: 前往 [google api](https://ai.google.dev/gemini-api/docs/pricing?hl=zh-tw) 查詢模型名稱。
*   **設定**: 選擇 `Google Gemini`，並將 API Key 與 model name 貼入對應欄位。

### 2. OpenAI (GPT-3.5/4)
*   **取得金鑰**: 前往 OpenAI Platform 申請。
*   **設定**: 選擇 `OpenAI (GPT)`，並貼入 API Key (需注意 OpenAI API 為付費服務)。

### 3. Local LLM (LM Studio)
*   **用途**: 在本地電腦執行模型，完全免費且隱私。
*   **設定**:
    1.  下載並安裝 LM Studio。
    2.  開啟 Local Server 功能 (預設 Port `1234`)。
    3.  在 App 中選擇 `Local (LM Studio)`。
    4.  輸入伺服器位址 (模擬器用 `http://10.0.2.2:1234`，實機用電腦 IP)。

## 🍎 iOS App 安裝流程 (iOS Installation)

**⚠️ 注意**: 由於 Apple 的限制，編譯與安裝 iOS App **必須** 在 macOS 環境下進行。如果您使用的是 Windows，無法直接產出 iOS 安裝檔 (.ipa)。

### 🛠️ 必備設備 (Requirements)
1.  **Mac 電腦**: MacBook, iMac 或 Mac mini (需安裝 macOS)。
2.  **Xcode**: 請至 Mac App Store 免費下載 (這是 iOS 的開發工具)。
3.  **Apple ID**: 用於開發者簽章 (免費帳號即可進行實機測試，若要上架需付費)。
4.  **iPhone / iPad**: (可選) 用於實機測試，若無實機也可使用 Xcode 內建的模擬器。

### 🚀 安裝步驟 (Steps on Mac)

1.  **環境準備**:
    確保 Mac 上已安裝 Flutter SDK 與 CocoaPods (iOS 的套件管理工具)。
    ```bash
    flutter doctor
    ```

2.  **安裝依賴**:
    進入專案目錄並安裝 iOS 相關依賴。
    ```bash
    cd flutter_app1
    flutter pub get
    cd ios
    pod install
    cd ..
    ```

3.  **Xcode 設定 (簽章)**:
    1.  在終端機執行 `open ios/Runner.xcworkspace` 開啟 Xcode。
    2.  點擊左側導覽列最上方的 **Runner** (藍色圖示)。
    3.  選擇 **Signing & Capabilities** 分頁。
    4.  在 **Team** 下拉選單中，登入並選擇您的 Apple ID (Personal Team)。
    5.  若出現 Bundle Identifier 錯誤，請修改 **Bundle Identifier** 為唯一名稱 (例如 `com.yourname.mooddiary`)。

4.  **執行 App**:
    *   **模擬器**: 選擇模擬器裝置 (如 iPhone 15)，點擊 Xcode 左上角的 ▶️ (Run) 按鈕。
    *   **實機**:
        1.  使用傳輸線連接 iPhone 至 Mac。
        2.  在 Xcode 頂部選擇您的裝置。
        3.  點擊 ▶️ (Run) 按鈕。
        4.  (首次執行) 在 iPhone 上前往 **設定 > 一般 > VPN 與裝置管理**，信任您的開發者 App 憑證。

## 🐛 問題回報 (Bug Report)
歡迎通過以下方式聯繫我：
*   Gmail: datayen7@gmail.com
